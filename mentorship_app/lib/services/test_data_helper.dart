import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:uuid/uuid.dart';

class TestDataHelper {
  // Dummy data seeding methods have been removed to prevent accidental usage.
  // This class is kept as a placeholder if future non-dummy utilities are needed.
}
